[package]
name = "dol-builder"
version.workspace = true
edition.workspace = true
publish = false
description = "DOL builder API — composable method-chain builders that produce IR."

[dependencies]
dol-expr = { path = "../dol-expr" }
dol-model = { path = "../dol-model" }
dol-ir = { path = "../dol-ir" }

