//! Tests for [`Value`] classification helpers (`is_*`, `type_name`).
//! Currently no dedicated tests live here; classification is exercised
//! indirectly by the conformance suite in [`crate::data_type`].
