//! # `dol-query` — Query Builder DSL
//!
//! M0 scaffold. The real content (Query / Select / Insert / Update / Delete
//! / Upsert builders, `try_build`, `BuildError`) lands in M5 per
//! `dol-rewrite-plan-v2.md` §10.
//!
//! The previous v0.1 implementation is preserved on disk in `_legacy_src/`
//! and `_legacy_tests/` as a reference to mine from.
#![no_std]
#![cfg_attr(docsrs, feature(doc_cfg))]
