//! Tests for [`super::super::DataType`] classification helpers
//! (`is_*` and `type_name`). Currently no tests live here; the file exists
//! to keep the per-concern layout symmetric and to give the obvious place
//! for new classify-only tests to land.
