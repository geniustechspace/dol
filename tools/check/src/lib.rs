//! # `dol-check` — Static validator
//!
//! M0 scaffold. The real validator (type / schema / capability / lint
//! passes, SARIF reporter) rebuilds atop `dol-ir` during M3+; legacy
//! source is preserved in `_legacy_src/` / `_legacy_tests/`.
#![no_std]
