[package]
name = "dol-core"
version.workspace = true
edition.workspace = true
publish = false
description = "DOL Core — type system, expressions, IR, Backend trait, and constraint types."

[dependencies]
smallvec = { workspace = true }
serde    = { workspace = true, optional = true }

[features]
default = []
serde = ["dep:serde"]
