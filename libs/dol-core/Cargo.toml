[package]
name = "dol-core"
version.workspace = true
edition.workspace = true
publish = false
description = "DOL Core — the complete language layer (expressions, models, IR, builders, Backend trait)."

[dependencies]
dol-expr = { path = "crates/dol-expr" }
dol-model = { path = "crates/dol-model" }
dol-ir = { path = "crates/dol-ir" }
dol-builder = { path = "crates/dol-builder" }
