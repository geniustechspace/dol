[package]
name = "dol-entity"
version.workspace = true
edition.workspace = true
publish = false
description = "DOL schema language — Entity, Field, FieldType, and constraints."

[dependencies]
serde = { workspace = true, optional = true }

[dev-dependencies]
serde_json = { workspace = true }

[features]
default = []
serde = ["dep:serde"]
