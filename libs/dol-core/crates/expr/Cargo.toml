[package]
name = "dol-expr"
version.workspace = true
edition.workspace = true
publish = false
description = "DOL expression engine — composable, backend-agnostic expression AST."

[dependencies]
dol-types = { path = "../types" }
serde = { workspace = true, optional = true }

[features]
default = []
serde = ["dep:serde", "dol-types/serde"]
