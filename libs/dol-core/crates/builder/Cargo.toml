[package]
name = "dol-builder"
version.workspace = true
edition.workspace = true
publish = false
description = "DOL builder API — EntityBuilderExt trait and DDL builders."

[dependencies]
dol-core = { path = "../../../dol-core" }
dol-entity = { path = "../entity" }
dol-query = { workspace = true }
