[package]
name = "dol-types"
description = "DOL type system — DataType descriptors, Value/Literal data carriers, validated primitives."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
serde = { workspace = true, optional = true }

[features]
default = []
serde = ["dep:serde"]
