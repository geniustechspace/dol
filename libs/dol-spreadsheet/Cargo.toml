[package]
name = "dol-spreadsheet"
version.workspace = true
edition.workspace = true
publish = false
description = "DOL spreadsheet backend — renders IR into spreadsheet operation descriptors."

[dependencies]
dol-core = { workspace = true }
serde = { workspace = true, optional = true }

[features]
default = []
serde = ["dep:serde", "dol-core/serde"]
