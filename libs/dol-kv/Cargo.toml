[package]
name = "dol-kv"
version.workspace = true
edition.workspace = true
publish = false
description = "DOL key-value backend — renders IR into KV operation descriptors."

[dependencies]
dol-ir  = { workspace = true }
serde   = { workspace = true, optional = true }

[features]
default = []
serde = ["dep:serde", "dol-ir/serde"]

[dev-dependencies]
dol-query = { workspace = true }
