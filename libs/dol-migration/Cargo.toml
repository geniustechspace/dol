[package]
name = "dol-migration"
version.workspace = true
edition.workspace = true
publish = false
description = "DOL migration system — type-safe schema migrations across all storage backends."

[dependencies]
dol-core = { workspace = true }
dol-sql = { workspace = true }
dol-config = { workspace = true, optional = true }

[dev-dependencies]
toml_crate = { workspace = true }

[features]
default = []
config = ["dep:dol-config"]
