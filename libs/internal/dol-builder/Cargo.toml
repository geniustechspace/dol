[package]
name = "dol-builder"
version.workspace = true
edition.workspace = true
publish = false
description = "DOL fluent builder API → produces IR (internal)"

[dependencies]
dol-expr = { path = "../dol-expr" }
dol-ir   = { path = "../dol-ir" }
smallvec = { workspace = true }

[features]
default = []
