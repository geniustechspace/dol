[package]
name = "dol-ir"
version.workspace = true
edition.workspace = true
publish = false
description = "DOL IR — Statement enum and Backend trait (internal)"

[dependencies]
dol-expr = { path = "../dol-expr" }
smallvec = { workspace = true }
serde    = { workspace = true, optional = true }

[features]
default = []
serde = ["dep:serde", "dol-expr/serde"]
