[package]
name = "xtask"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false
description = "DOL — workspace task runner (size, nostd, doc, audit)."
readme = "README.md"

[dependencies]
dol-types = { workspace = true }
dol-expr  = { workspace = true }
dol-ir    = { workspace = true }
